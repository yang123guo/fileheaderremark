
class RenderTpl {
    constructor(props) {
        const {
            vscode,
            ...config
        } = props;
        this.init(); // 执行方法
    }

    init() {
        return 'hahha'
    }
};

export default RenderTpl;