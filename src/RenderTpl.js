
class RenderTpl {
    constructor(props) {
        const {
            vscode,
            ...config
        } = props;
        this.init(); // 执行方法
    }

    init() {

    }
};

export default RenderTpl;